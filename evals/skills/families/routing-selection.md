# Routing Selection

Primary skill: `epistemic-routing`

Reviewer question: does the workflow select routing review when an intervention cycle needs attention allocation, and does it refuse routing when the task is baseline release or non-ACP work?

Required signals: intervention status, routing review, bridge budget, omission/fairness check.
